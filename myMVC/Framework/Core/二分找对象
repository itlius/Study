<?php

int a ; int length; int low ;
foreach(a , low ,length -1){
	if(a[i] = a)
	{
		return 1;
	}
	if(a[i] <low){
	length/2 =length;
	return(length)
	}
}
二分查找方法
$min = 0;
$max = count($arr)-1;
while( $min <$max ){
	$middle = floor(( $min +$max) /2);

	找到元素
	if($arr[$middle] == $search){
	echo'ok';retun;
	}

	如果存在中间元素 比目标小
	if( $arr[$middle] < $search ){
	$mi = $middle +1;
	}
	
	如果中间元素比目标大、
	if( $arr[middle] >$search ){
	$min = $middle +1 ;

	}
	echo'no find';rutun;
}

//字符串反转
$str="abcdfe0";
$a="";
for(lenth($str);$i>-1;$i--){
	$str[$i].=$str;
}

//linux定时任务
crontab 

service crond start启动命令

安装前（安装 vixie-cron crontabs）

-e编辑文件 crontab -l列内容
-r 删除文件


      可用crontab -e命令来编辑,编辑的是/var/spool/cron下对应用户的cron文件,也可以直接修改/etc/crontab文件
     具体格式如下：
      Minute Hour Day Month Dayofweek   command
      分钟     小时   天     月       天每星期       命令
     每个字段代表的含义如下：
     Minute             每个小时的第几分钟执行该任务
     Hour               每天的第几个小时执行该任务
     Day                 每月的第几天执行该任务
     Month             每年的第几个月执行该任务
     DayOfWeek     每周的第几天执行该任务
     Command       指定要执行的程序




